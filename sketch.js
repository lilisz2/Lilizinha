function setup() {
  createCanvas(600, 600);
  background("red");
}

function draw() {
  stroke("blue");
  fill("black");

  // consele. log(mousseIspressed)

if (mouseIsPressed) {
  rect(mouseX, mouseY, 20, 35);
   }
}
